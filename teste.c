#include <stdio.h>

int main () { 





// Um array de 5 inteiros ocupando um bloco contínuo de memória
int numeros[5] = {10, 20, 30, 40, 50};

// Acesso direto e super rápido via índice:
printf("%d\n", numeros[2]); // Imprime 30

return 0;

}